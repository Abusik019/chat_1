// Online C++ compiler to run C++ program online
#include <iostream>;
#include <emath>;

int main() {
    double a,y,e,x,S
    cin>>a>>y>>e>>x;
    S=sqrt((sqrt(pow(a,4)+poq(sin(y),2)))/(pow(e,x)+log(x)));
    cout<<S;
    return 0;
}